<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidfundamentals04">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReceiverActivity"></activity>
        <activity android:name=".SenderActivity"></activity>
        <activity android:name=".text_display.LaunchedActivity" />
        <activity android:name=".text_display.DisplayActivity" />
        <activity android:name=".ImplicitTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".lifecycle_activity.SecondActivity" />
        <activity android:name=".lifecycle_activity.LifecycleActivity" />
        <activity android:name=".student_recycler.StudentActivity" />
        <activity android:name=".SpinnerActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>